<configuration>

    <!-- Rolling File Appender en JSON -->
    <appender name="FILE_JSON" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/var/log/springboot/app.json</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Archivos rotados por fecha y tamaño -->
            <fileNamePattern>/var/log/springboot/archived/sysred.%d{yyyy-MM-dd}.%i.json</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>

        <!-- Encoder JSON -->
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <timeZone>America/Bogota</timeZone>
            <includeMdc>true</includeMdc>
        </encoder>
    </appender>

    <!-- Console Appender en texto normal (opcional) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} [operation=%X{operation} response=%X{response}] - %msg%n</pattern>        </encoder>
    </appender>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="FILE_JSON" />
        <appender-ref ref="CONSOLE" />
    </root>

</configuration>